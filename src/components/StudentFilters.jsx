import { Search } from 'lucide-react'
import { programmes, yearsOfStudy, thesisStages, studentStatuses } from '../utils/studentUtils'
export default function StudentFilters({filters,setFilters}){const set=(key,value)=>setFilters({...filters,[key]:value});return <div className="student-filters card"><label className="student-search"><Search size={17}/><input aria-label="Search students" value={filters.search} onChange={e=>set('search',e.target.value)} placeholder="Search name, email, thesis title…"/></label><select aria-label="Programme" value={filters.programme} onChange={e=>set('programme',e.target.value)}><option value="all">All programmes</option>{programmes.map(x=><option key={x}>{x}</option>)}</select><select aria-label="Year" value={filters.year} onChange={e=>set('year',e.target.value)}><option value="all">All years</option>{yearsOfStudy.map(x=><option key={x}>{x}</option>)}</select><select aria-label="Stage" value={filters.stage} onChange={e=>set('stage',e.target.value)}><option value="all">All stages</option>{thesisStages.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select><select aria-label="Status" value={filters.status} onChange={e=>set('status',e.target.value)}><option value="all">All statuses</option>{studentStatuses.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select><select aria-label="Sort" value={filters.sort} onChange={e=>set('sort',e.target.value)}><option value="deadline">Sort: deadline</option><option value="consultation">Sort: last consultation</option><option value="name">Sort: name</option><option value="status">Sort: status</option></select></div>}
