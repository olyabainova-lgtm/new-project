const statusColors={pending:'amber',approved:'purple',rejected:'rose',completed:'green'}
export default function MeetingStatusReport({counts}){const total=Object.values(counts).reduce((a,b)=>a+b,0);return <section className="card report-section"><span className="eyebrow">Request outcomes</span><h2>Meeting status</h2><div className="status-report">{Object.entries(counts).map(([status,count])=><div key={status}><span><strong>{status}</strong><small>{count} meeting{count===1?'':'s'}</small></span><div className="report-bar"><i className={`report-${statusColors[status]}`} style={{width:`${total?count/total*100:0}%`}}/></div><b>{total?(count/total*100).toFixed(0):0}%</b></div>)}</div></section>}
