import EmailStatusBadge from './EmailStatusBadge'
export default function EmailHistory({logs=[]}){return <details className="email-history"><summary>Message history ({logs.length})</summary>{logs.length?<div>{[...logs].reverse().map(log=><article key={log.id}><span><strong>{log.type}</strong><EmailStatusBadge status={log.status}/></span><small>To: {log.recipientEmail}</small><small>{new Intl.DateTimeFormat('en',{dateStyle:'medium',timeStyle:'short'}).format(new Date(log.sentAt))}</small>{log.errorMessage&&<p>{log.errorMessage}</p>}</article>)}</div>:<p>No automatic messages sent yet.</p>}</details>}
