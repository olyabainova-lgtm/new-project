import { useState } from 'react'
export default function ConsultationNoteForm({onAdd}){const [open,setOpen]=useState(false),[form,setForm]=useState({date:new Date().toISOString().slice(0,10),time:'',topic:'',format:'offline',teacherComment:'',nextStep:''});const submit=e=>{e.preventDefault();if(!form.date||!form.topic.trim())return;onAdd(form);setOpen(false);setForm({...form,topic:'',teacherComment:'',nextStep:''})};return open?<form className="consultation-form" onSubmit={submit}><div className="field-grid"><label>Date *<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></label><label>Time<input type="time" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/></label></div><label>Topic *<input value={form.topic} onChange={e=>setForm({...form,topic:e.target.value})}/></label><label>Format<select value={form.format} onChange={e=>setForm({...form,format:e.target.value})}><option value="offline">Offline</option><option value="online">Online</option></select></label><label>Private consultation note<textarea rows="2" value={form.teacherComment} onChange={e=>setForm({...form,teacherComment:e.target.value})}/></label><label>Next step<textarea rows="2" value={form.nextStep} onChange={e=>setForm({...form,nextStep:e.target.value})}/></label><div className="form-actions"><button className="btn btn-primary btn-sm">Save consultation</button><button type="button" className="btn btn-ghost btn-sm" onClick={()=>setOpen(false)}>Cancel</button></div></form>:<button className="btn btn-secondary btn-sm" onClick={()=>setOpen(true)}>Add manual consultation</button>}
