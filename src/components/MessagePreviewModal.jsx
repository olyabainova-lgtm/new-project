import { Copy, X } from 'lucide-react'
export default function MessagePreviewModal({subject,body,onClose,onCopy,onSend,sending}){return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="modal-card card message-preview"><button className="modal-close" onClick={onClose}><X/></button><span className="eyebrow">Message preview</span><h2>{subject}</h2><pre>{body}</pre><div className="preview-actions"><button className="btn btn-ghost" onClick={onCopy}><Copy size={16}/>Copy message</button><button className="btn btn-primary" onClick={onSend} disabled={sending}>{sending?'Sending…':'Send email'}</button></div></section></div>}
