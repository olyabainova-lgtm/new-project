import { workloadCategories } from '../utils/reportUtils'
export default function WorkloadByCategory({workload,total}){const max=Math.max(...Object.values(workload),1);return <section className="card report-section"><div className="report-section-head"><div><span className="eyebrow">Time allocation</span><h2>Workload by category</h2></div><strong>{total.toFixed(1)} hours</strong></div><div className="workload-bars">{workloadCategories.map(item=>{const hours=workload[item.key]||0,percentage=total?hours/total*100:0;return <div className="workload-row" key={item.key}><span className={`workload-dot occupation-${item.color}`}/><strong>{item.label}</strong><div className="report-bar"><i className={`occupation-${item.color}`} style={{width:`${hours/max*100}%`}}/></div><span>{hours.toFixed(1)}h</span><small>{percentage.toFixed(0)}%</small></div>})}</div></section>}
